@page
@model IndexModel
@{
    ViewData["Title"] = "Settings";
}

<h1>Settings <small class="text-muted">Page</small></h1>

<div class="row">
    <div class="col-lg-6">
        <h2>Module Category Weights</h2>
        @if (Model.ModuleCategories.Any())
        {
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th scope="col">Category</th>
                        <th scope="col">Weight</th>                        
                        <th scope="col"></th>

                    </tr>
                </thead>
                <tbody>
                    @foreach (var mi in Model.ModuleCategories)
                    {
                        <tr>
                            <td>@mi.Category</td>
                            <td>@mi.Weight</td>                            
                            <td><a asp-page="EditCategoryWeight" asp-route-id="@mi.Category">Edit</a></td>
                        </tr>
                    }
                </tbody>
            </table>
            <h6>@Model.ModuleCategories.Count() module categories.</h6>
        }
        else
        {
            <h6>Categories assigned to modules appear here.</h6>
        }
    </div>
</div>

<div class="row">
    <div class="col-lg-6">
        <h2>Attorney Fees</h2>
        @if (Model.AttorneyFees.Any())
        {
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th scope="col">Attorney Fee Level</th>
                        <th scope="col">Amount</th>
                        <th scope="col">Currency</th>
                        <th scope="col"></th>

                    </tr>
                </thead>
                <tbody>
                    @foreach (var mi in Model.AttorneyFees)
                    {
                        <tr>
                            <td>@mi.FeeLevel</td>
                            <td>@mi.Amount</td>
                            <td>@mi.Currency</td>
                            <td><a asp-page="EditAttorneyFee" asp-route-id="@mi.FeeLevel">Edit</a></td>
                        </tr>
                    }
                </tbody>
            </table>
            <h6>@Model.AttorneyFees.Count() attorney fee levels.</h6>
        }
        else
        {
            <h6>Attorney fees appear here.</h6>
        }
    </div>
</div>